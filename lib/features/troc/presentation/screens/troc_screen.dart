import 'package:flutter/material.dart';

/// Vrai troc à la burkinabè
class TrocScreen extends StatelessWidget {
  const TrocScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Troc Screen'),
      ),
    );
  }
}

