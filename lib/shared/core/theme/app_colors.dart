import 'package:flutter/material.dart';

/// Palette officielle ECONOMAX 🇧🇫
/// Inspirée gobelet café/thé du coin
class AppColors {
  AppColors._();

  // Primary (marron café brûlé)
  static const Color primary = Color(0xFF8B4513);
  static const Color primaryDark = Color(0xFF6B3A0A);

  // Secondary (beige crème de lait)
  static const Color secondary = Color(0xFFD7CCC8);

  // Success (vert drapeau Burkina)
  static const Color success = Color(0xFF009E60);

  // Warning (jaune/orange piment)
  static const Color warning = Color(0xFFFF9800);

  // Error (rouge drapeau)
  static const Color error = Color(0xFFEF3340);

  // Background & Surface
  static const Color background = Color(0xFFFAF9F6);
  static const Color surface = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF5D4037);

  // On Primary
  static const Color onPrimary = Color(0xFFFFFFFF);
}

